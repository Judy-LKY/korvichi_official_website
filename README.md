# Korvichi Website V12 Fixed

Image path fixed version. All images are referenced from the repository root, matching the current GitHub upload structure.

Upload all files directly to the repository root. Do not upload the outer folder.
